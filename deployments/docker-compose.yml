version: "3"
services:
  mongo:
    image: mongo:4
    restart: on-failure
    networks:
      - backend

  api:
    image: "client-api"
    ports:
      - "8080:8080"
    restart: on-failure
    depends_on:
      - repository
    networks:
      - backend
    volumes:
      - ${PWD}/../data/:/opt/api/data/

  repository:
    image: "port-domain-svc"
    restart: on-failure
    depends_on:
      - mongo
    networks:
      - backend

networks:
  backend:
    driver: bridge